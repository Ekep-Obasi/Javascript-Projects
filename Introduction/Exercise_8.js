// Javascript program that works with constants e.g to Calculate area of a circle
const pie = 3.14;
let radius = 3;
let area = pie * (radius * radius);
console.log(`The area of a circle with radius ${radius} is ${area} units`);
